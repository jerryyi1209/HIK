# CarpetSegNet v2 — D435 Inference Requirements
#
# Jetson Orin Nano (JetPack 6.x / Ubuntu 22.04):
#   PyTorch and torchvision are pre-installed via JetPack.
#   DO NOT pip install torch/torchvision — use the system packages.
#
# Install:
#   pip install -r requirements.txt

numpy>=1.21
opencv-python>=4.8

# ONNX Runtime (optional — for ONNX inference)
# On Jetson: use the JetPack-bundled onnxruntime or install:
#   pip install onnxruntime-gpu
# onnxruntime>=1.16

# ONNX export tools (optional — for model export, not needed for inference)
# torch>=2.0  # pre-installed via JetPack
# onnx>=1.14
# onnxsim>=0.4  # optional graph optimization
